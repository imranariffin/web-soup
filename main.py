from command_parser import command_parser, main_parser

global web_page
web_page = "imma web page"

# command_line = raw_input("> ")
# while command_line != "exit":
# 	command_line = raw_input("> ")
# 	command_parser(command_line)

main_parser()
